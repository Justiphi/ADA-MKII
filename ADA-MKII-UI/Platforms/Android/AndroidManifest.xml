<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!--
		networkSecurityConfig keeps cleartext HTTP off everywhere except the
		development hosts listed in that file. Without it, Android 9+ silently
		refuses plain-HTTP requests and the app looks like it cannot reach the
		server at all.
	-->
	<application android:allowBackup="true"
	             android:icon="@mipmap/appicon"
	             android:roundIcon="@mipmap/appicon_round"
	             android:supportsRtl="true"
	             android:networkSecurityConfig="@xml/network_security_config"></application>

	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />

	<!--
		Package visibility. Without this, SpeechRecognizer silently finds no
		service on API 30+ and recognition fails with no useful error. Kept even
		though recognition currently runs through Whisper on-device, since a
		platform-API engine remains a live option.
	-->
	<queries>
		<intent>
			<action android:name="android.speech.RecognitionService" />
		</intent>
	</queries>
</manifest>
